class AppConstants {
  AppConstants._();

  static const String appName = '团队技术展示门户';
  static const String repoName = 'group-flutter-pages-demo';
  static const String githubOrg = 'umm0v0';

  // Responsive breakpoints
  static const double mobileBreakpoint = 600;
  static const double tabletBreakpoint = 1024;
  static const double contentMaxWidth = 1200;

  // Animation
  static const Duration pageTransitionDuration = Duration(milliseconds: 300);
  static const Duration staggerDelay = Duration(milliseconds: 80);
  static const Duration hoverDuration = Duration(milliseconds: 200);
}
